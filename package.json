{
  "name": "opentrade",
  "version": "0.0.0",
  "private": true,
  "license": "Elastic-2.0",
  "type": "module",
  "packageManager": "bun@1.3.14",
  "workspaces": ["app"],
  "scripts": {
    "dev": "bun run --cwd app dev",
    "build": "bun run --cwd app build",
    "typecheck": "bun run --cwd app typecheck",
    "test": "bun test",
    "lint": "bunx @biomejs/biome check .",
    "format": "bunx @biomejs/biome format --write .",
    "prototype:candidate-review": "python3 -m http.server 4174 --bind 127.0.0.1 --directory prototypes/candidate-review",
    "prototype:candidate-review-adapted": "python3 -m http.server 4175 --bind 127.0.0.1 --directory prototypes/candidate-review-adapted"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.2",
    "typescript": "5.9.2"
  },
  "trustedDependencies": ["better-sqlite3", "electron", "esbuild", "node-pty"]
}
